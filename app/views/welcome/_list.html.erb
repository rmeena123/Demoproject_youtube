  <% @posts = @category.posts%>
  <% @posts = @posts.reject{|post| post.id == @post.id}%> <%# They are rejected after play video inside show bar %>
  <% @posts.each do |post| %> 
     <div class="text-black zoom hover p-3" style="width: 20rem">
       <%= link_to(image_tag(post.image, size: "300x200"),welcome_path(post)) %>
       <%= @category.name%> 
       </br> 
     </div> 
 <%end%>


     
         
         
           

  
  